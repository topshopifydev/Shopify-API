{
  "name": "shopify-counter",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3",
    "vercel": "^46.1.1"
  }
}
