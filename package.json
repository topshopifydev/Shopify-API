{
  "name": "shopify-counter",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vercel": "^46.1.1"
  }
}
